Nivel profesional (full stack + Mysql +  Docker) usando micro-servicios:

### 🧠 API → TypeScript (Node.js)
### 🌐 Web → Django (MVC)
### 🗄️ DB → MySQL
### 🐳 Docker Compose (arquitectura desacoplada)

## Step 1: create structure of project

Replace the word “project” with whatever name you prefer.

```
mkdir proyecto && cd proyecto

mkdir api web
touch docker-compose.yml
```
Follow the same steps to create a folder named “api” and, within it, the file “docker-compose.yml”

## Step 2: 

Organize the folders according to the folder structure already defined in the documentation
- [structure](structure.md)